based on .github/product.md, implement the next unimplemented phase and mark it complete when done (don't commit to git though). you can verify compatibility with the official JQ implementation by running the installed jq.exe (already in PATH) as needed. the codebase is documented in .github/design.md, use it to understand the design and implementation details and keep it up to date as you make changes.
